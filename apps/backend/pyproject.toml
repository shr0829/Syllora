[project]
name = "syllora"
version = "0.1.0"
description = "AI learning workspace for structured learning paths, staged lessons, and multi-image study assets"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []
